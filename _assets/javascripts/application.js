//= require components/navbar

// Add .is-loaded when page is ready, helpful for animation/analytics triggers
window.onload = function () {
  document.body.className = document.body.className.trim() + ' is-loaded';
};
